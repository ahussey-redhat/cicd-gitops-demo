---
- name: Bootstrap ArgoCD
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Install ArgoCD
      kubernetes.core.k8s:
        template: "install-argocd.yml"
